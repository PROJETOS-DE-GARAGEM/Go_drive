import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  containerImage: {
    display: "flex",
    marginTop: 15,
  },
    carousel: {
      display: "flex",
      marginTop: 15,
  }
});

export default styles;