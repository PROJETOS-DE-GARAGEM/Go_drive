import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  formSpacing: {
    marginBottom: -24, // Espaçamento entre o primeiro e o segundo formulário
  },
  row: {
    flexDirection: "row", // Alinha os campos horizontalmente
    justifyContent: "space-between", // Espaça os campos igualmente

  },
  
});

export default styles;
