import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  textTerm: {
    textAlign: "justify",
    fontSize: 16,
  },
  topicItem: {
    fontSize: 14,
    fontWeight: "bold",
  },
});

export default styles;
